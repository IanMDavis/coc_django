# Call of Cthulhu - Django Edition!

## Base install steps:
* Clone this repo
* Create a virtual environment preferably OUTSIDE of the repo
    * python3 -m venv path/to/venv/dir
* Install django
    * pip3 install django
